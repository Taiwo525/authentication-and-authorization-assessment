spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: true
    locations: classpath:db/migration/h2

security:
  starter:
    jwt-secret: Kx9vPmQr3sT7wYzA2bCdEfGhJkLnMpRtUvWx4yZ6aBcDeFgH
    token-expiry-seconds: 3600
    token-issuer: test-application
